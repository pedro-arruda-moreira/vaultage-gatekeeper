<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
						http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<http use-expressions="true" >
		<csrf disabled="true"/>
		<intercept-url pattern="/**" access="isAuthenticated()" />
		<http-basic />
	</http>
	
	<context:property-placeholder location="file:${user.home}/vaultage-security-facade.properties"/>
	
	<beans:bean class="com.github.pedroarrudamoreira.vaultage.root.listener.Starter">
		<beans:property name="users" value="${users}" />
		<beans:property name="port" value="${port:3000}" />
	</beans:bean>
	<beans:bean class="com.github.pedroarrudamoreira.vaultage.root.servlet.MobileServlet"
		id="mobileServlet">
	</beans:bean>
	<beans:bean class="com.github.pedroarrudamoreira.vaultage.accesscontrol.SessionController">
		<beans:property name="maxSessionsPerDay" value="${max-sessions-per-day:5}" />
		<beans:property name="maxSessionsPerHour" value="${max-sessions-per-hour:3}" />
		<beans:property name="maxLoginAttemptsPerSession" value="${max-attempts-per-session:3}" />
		<beans:property name="sessionDurationInHours" value="${session-duration-in-hours:72}" />
		<beans:property name="secure" value="${https-enabled:true}" />
	</beans:bean>
	

	<authentication-manager>
		<authentication-provider>
			<user-service>
				<user name="${basic_user}" password="{noop}${basic_password}"
					authorities="ROLE_USER" />
			</user-service>
		</authentication-provider>
	</authentication-manager>

</beans:beans>